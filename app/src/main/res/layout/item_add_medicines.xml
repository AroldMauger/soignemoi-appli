<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/edit_text_background">

    <!-- Close Button -->
    <ImageView
        android:id="@+id/close_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/close"
        android:contentDescription="@string/close"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/time_modal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Traitement / Médicament"
        android:layout_marginTop="18dp"
        android:layout_marginBottom="8dp"
        android:textSize="20dp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@id/close_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/reason_modal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Posologie"
        android:layout_marginBottom="8dp"
        android:textSize="20dp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@id/time_modal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/car_modal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Date de début du traitement"
        android:layout_marginBottom="8dp"
        android:textSize="20dp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@id/reason_modal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/customer_modal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Date de fin du traitement"
        android:layout_marginBottom="8dp"
        android:textSize="20dp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@id/car_modal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
